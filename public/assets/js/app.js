(()=>{const body=document.body;const navToggle=document.querySelector('[data-nav-toggle]');const closeNav=()=>{body.classList.remove('nav-open');navToggle?.setAttribute('aria-expanded','false')};navToggle?.addEventListener('click',()=>{const open=!body.classList.contains('nav-open');body.classList.toggle('nav-open',open);navToggle.setAttribute('aria-expanded',open?'true':'false')});document.querySelectorAll('[data-nav-close]').forEach(el=>el.addEventListener('click',closeNav));document.querySelectorAll('.sidebar a').forEach(link=>link.addEventListener('click',()=>{if(window.matchMedia('(max-width: 900px)').matches)closeNav()}));window.addEventListener('resize',()=>{if(window.innerWidth>900)closeNav()});const menu=document.querySelector('[data-account-menu]');const toggle=menu?.querySelector('[data-account-toggle]');const dropdown=menu?.querySelector('[data-account-dropdown]');const closeAccount=()=>{dropdown?.setAttribute('hidden','');toggle?.setAttribute('aria-expanded','false')};toggle?.addEventListener('click',()=>{const open=dropdown?.hasAttribute('hidden');if(open)dropdown?.removeAttribute('hidden');else dropdown?.setAttribute('hidden','');toggle.setAttribute('aria-expanded',open?'true':'false')});document.addEventListener('click',e=>{if(menu&&!menu.contains(e.target))closeAccount()});document.querySelectorAll('[data-password-toggle]').forEach(btn=>btn.addEventListener('click',()=>{const input=document.getElementById(btn.dataset.passwordToggle);if(!input)return;const show=input.type==='password';input.type=show?'text':'password';btn.setAttribute('aria-label',show?'Hide password':'Show password')}));const dialog=document.querySelector('[data-confirm-dialog]');const message=dialog?.querySelector('[data-confirm-message]');const cancel=dialog?.querySelector('[data-confirm-cancel]');const proceed=dialog?.querySelector('[data-confirm-proceed]');let pending=null;document.querySelectorAll('form[data-confirm]').forEach(form=>form.addEventListener('submit',e=>{if(form.dataset.confirmed==='1')return;e.preventDefault();pending=form;if(message)message.textContent=form.dataset.confirm||'Are you sure?';dialog?.showModal()}));cancel?.addEventListener('click',()=>{dialog?.close();pending=null});proceed?.addEventListener('click',()=>{if(!pending)return;pending.dataset.confirmed='1';dialog?.close();pending.requestSubmit()});const items=document.querySelector('[data-items]');const tpl=document.querySelector('[data-item-template]');document.querySelector('[data-add-item]')?.addEventListener('click',()=>{if(items&&tpl)items.append(tpl.content.cloneNode(true))});items?.addEventListener('click',e=>{const btn=e.target.closest('[data-remove-item]');if(btn&&items.querySelectorAll('.request-item').length>1)btn.closest('.request-item')?.remove()});document.querySelectorAll('[data-dialog-open]').forEach(btn=>btn.addEventListener('click',()=>document.getElementById(btn.dataset.dialogOpen)?.showModal()));document.querySelectorAll('[data-dialog-close]').forEach(btn=>btn.addEventListener('click',()=>btn.closest('dialog')?.close()));document.querySelectorAll('dialog.form-dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}));document.querySelectorAll('form:not([data-confirm])').forEach(form=>form.addEventListener('submit',()=>{const submit=form.querySelector('button[type="submit"],input[type="submit"]');if(submit){submit.disabled=true;submit.setAttribute('aria-disabled','true')}}));window.addEventListener('keydown',e=>{if(e.key==='Escape'){closeNav();closeAccount()}})})();
